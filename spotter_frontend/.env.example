# Copy this to .env.local for local dev, or set in Vercel dashboard for production

# Your Railway backend URL (set this in Vercel dashboard → Settings → Environment Variables)
VITE_API_URL=https://your-app.up.railway.app

# Optional: Mapbox token for premium map tiles
# VITE_MAPBOX_TOKEN=pk.eyJ1...
